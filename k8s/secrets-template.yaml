apiVersion: v1
kind: Secret
metadata:
  name: retail-secrets
type: Opaque
stringData:
  # Replace with actual values
  # Kubernetes expects base64 encoded data if using 'data' field,
  # or plain text if using 'stringData' field (which is then encoded automatically).
  mongo-url: "mongodb://mongodb:27017" # Internal cluster DNS or external URI
  HUGGINGFACE_API_TOKEN: "hf_YOUR_TOKEN_HERE"
